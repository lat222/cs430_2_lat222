#ifndef RAYCAST
#define RAYCAST

#include <stdio.h>
#include <stdlib.h>

#include "linkedlist.h"

typedef struct vector
{
	int x;
	int y;
	int x;
} vector;

typedef struct object
{
	char* type;
	pixel* pix;
	vector* postion;

}

node* raycast(file* fp, width, height);

shoot();

shade();



#endif